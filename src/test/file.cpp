#include <catch.hpp>
#include "middleware/file.hpp"

GIVEN("a FileHostingSession is created") {
  WHEN("a database of FileBuckets exist") {
    THEN("it loads a directory of FileBuckets into memory")
  }

  WHEN("a ") {
    
  }

  WHEN("a ") {
    
  }
}

GIVEN("") {
  
}
